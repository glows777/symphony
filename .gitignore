# Secrets — never commit. The real live e2e reads LINEAR_API_KEY from the shell
# env; keep any local .env out of git.
.env
.env.*

# Local Symphony issue workspaces.
runtime/
typescript/frontend/dist/

# Local per-project workflow configurations.
projects/

.DS_STORE
